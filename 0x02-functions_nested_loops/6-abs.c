#include "main.h"
/**
 * _abs - to print the absolute value of a number
 * @n: the number to print
 * Return: the absolute value
 *
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
